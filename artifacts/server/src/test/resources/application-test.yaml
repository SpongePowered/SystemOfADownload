test-resources:
    containers:
        postgres:
            image-name: postgres:14.6
            username: testuser
            password: testpassword
            db-name: testdb
            port: 7654
liquibase:
    enabled: true
    datasources:
        default:
            change-log: 'classpath:db/test-changelog.xml'
endpoints:
    liquibase:
        enabled: true
        sensitive: false
micronaut:
    environment: test
    http:
        client:
            read-timeout: 5m
